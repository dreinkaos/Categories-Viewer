import { FilterOptionsByValuePipe } from './filter-options-by-value.pipe';

describe('FilterOptionsByValuePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterOptionsByValuePipe();
    expect(pipe).toBeTruthy();
  });
});
