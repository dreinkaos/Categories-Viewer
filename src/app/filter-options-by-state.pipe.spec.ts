import { FilterOptionsByStatePipe } from './filter-options-by-state.pipe';

describe('FilterOptionsByStatePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterOptionsByStatePipe();
    expect(pipe).toBeTruthy();
  });
});
